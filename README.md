# OtelRedisNative

Native OTel API Observability Platform built with Node.js, TypeScript, Express, Redis, and OpenTelemetry.

## Goal

Build a production-grade backend platform that demonstrates:

- Middleware architecture
- Redis-backed rate limiting
- Native observability (metrics and traces)
- Structured logging
- Resilience and performance engineering

## Tech Stack

- Node.js
- TypeScript
- Express
- Redis (ioredis)
- OpenTelemetry (planned in next phases)
- Pino logging
- Vitest, ESLint, Prettier

## Current Status

Project foundation is initialized:

- Dependencies installed
- TypeScript initialized
- Base folders created
- Core app files created
- Git repository initialized

## Prerequisites

- Node.js 20+
- npm 10+
- Redis 7+ (local Docker is fine)

## Quick Start (PowerShell)

Install dependencies:

    npm install

Create environment file:

    New-Item -ItemType File -Force -Path ".env"

Add this content to .env:

    PORT=4000
    REDIS_HOST=localhost
    REDIS_PORT=6379
    RATE_LIMIT_WINDOW=10
    RATE_LIMIT_MAX=5

Run development server:

    npm run dev

Build:

    npm run build

Start production build:

    npm run start

## Scripts

- dev: Run server with tsx watch mode
- build: Compile TypeScript with tsc
- start: Run compiled server from dist
- test: Placeholder script (to be replaced)

## Project Structure

    src/
      app/
      config/
      middleware/
        rateLimiter/
      telemetry/
      redis/
      metrics/
      logging/
      storage/
      observability/
      routes/
      services/
      utils/
      tests/
      scripts/
      dashboards/
    storage/
      logs/
      metrics/
      traces/

## Roadmap

Implementation follows:

1. Foundation and Express APIs
2. Typed env config and structured logs
3. Request correlation and Redis integration
4. Rate limiting (naive and sliding window)
5. OpenTelemetry instrumentation
6. Native metrics and trace pipelines
7. System monitoring and CLI dashboard
8. Load testing and resilience hardening

## Notes

- This repository currently uses a default tsconfig generated by tsc --init.
- Before production build flow is finalized, ensure tsconfig output directory aligns with npm start (dist/app/server.js).

## License

ISC
